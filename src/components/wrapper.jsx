import "./Wrapper.css";
import { Textfit } from "react-textfit";
import styled from "styled-components";

const Wrapper = ({ children }) => {
  <StyledTextfit className="wrapper">{children}</StyledTextfit>;
};

export default Wrapper;